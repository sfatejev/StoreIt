@model IEnumerable<Domain.Orders.OrderType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderTypeValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderTypeDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderTypeActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrderTypeValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderTypeDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderTypeActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderTypeId })
        </td>
    </tr>
}

</table>
