@model IEnumerable<Domain.Store.ProductCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductCategoryValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductCategoryDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductCategoryActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCategoryValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCategoryDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCategoryActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductCategoryId })
        </td>
    </tr>
}

</table>
