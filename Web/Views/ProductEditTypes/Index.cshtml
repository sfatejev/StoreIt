@model IEnumerable<Domain.Store.ProductEditType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductEditTypeValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductEditTypeActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductEditTypeValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductEditTypeActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductEditTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductEditTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductEditTypeId })
        </td>
    </tr>
}

</table>
