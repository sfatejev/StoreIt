@model IEnumerable<Domain.People.ContactType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContactTypeValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContactTypeActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContactTypeValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContactTypeActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContactTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContactTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContactTypeId })
        </td>
    </tr>
}

</table>
